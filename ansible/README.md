# Ansible automation

- `inventory/`: environment inventories without secrets.
- `roles/`: idempotent remediation roles.
- `common/`: shared tasks, variables, validation, and rollback helpers.

Use Ansible Vault or an approved secret manager for sensitive values.
